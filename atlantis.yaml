version: 3
projects:
  - name: k8s-infra
    dir: . # Корневая директория проекта
    workspace: default
    autoplan:
      when_modified: ["*.tf", "../modules/**/*.tf", "*.tfvars", ".terraform.lock.hcl"]
      enabled: true
