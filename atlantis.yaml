version: 3
projects:
  - name: k8s-infra
    dir: k8s-infra
    workspace: default
    autoplan:
      when_modified: ["*.tf", "../modules/**/*.tf", "*.tfvars", ".terraform.lock.hcl"]
      enabled: true
